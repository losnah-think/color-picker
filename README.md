# 효정이의 Pantone 컬러 팔레트 🎨

AI를 활용하여 실내 인테리어에 완벽한 Pantone 컬러 팔레트를 추천받는 웹 애플리케이션입니다.

## 주요 기능

- 🤖 **Google Gemini AI** 기반 컬러 팔레트 추천
- 🎨 **실제 Pantone 컬러 코드** 제공
- 🏠 다양한 인테리어 스타일 및 공간 유형 지원
- 💡 각 컬러별 사용처 및 설명 제공
- 📋 원클릭 컬러 코드 복사 기능
- 🌙 다크모드 지원

## 기술 스택

- **Frontend**: React, Next.js 14, TypeScript
- **Styling**: Tailwind CSS
- **AI**: Google Gemini API
- **Backend**: Next.js API Routes

## 설치 및 실행 방법

### 1. 의존성 설치

```bash
npm install
```

### 2. 환경 변수 설정

`.env.example` 파일을 복사하여 `.env.local` 파일을 생성하세요:

```bash
copy .env.example .env.local
```

`.env.local` 파일을 열고 Gemini API 키를 입력하세요:

```
GEMINI_API_KEY=your_actual_gemini_api_key
```

**Gemini API 키 발급 방법:**
1. [Google AI Studio](https://makersuite.google.com/app/apikey) 방문
2. Google 계정으로 로그인
3. "Create API Key" 클릭
4. 생성된 API 키 복사

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

### 4. 프로덕션 빌드

```bash
npm run build
npm start
```

## 사용 방법

1. **인테리어 스타일** 입력 (예: 모던, 북유럽, 미니멀)
2. **공간 유형** 선택 (거실, 침실, 주방 등)
3. **원하는 분위기** 입력 (선택사항)
4. "팔레트 생성하기" 버튼 클릭
5. AI가 추천한 5개의 Pantone 컬러 확인
6. 컬러 코드 클릭하여 복사

## 프로젝트 구조

```
Color-Picker/
├── app/
│   ├── api/
│   │   └── generate-palette/
│   │       └── route.ts          # Gemini API 통합
│   ├── globals.css               # 전역 스타일
│   ├── layout.tsx                # 루트 레이아웃
│   └── page.tsx                  # 메인 페이지
├── components/
│   └── ColorPalette.tsx          # 컬러 팔레트 컴포넌트
├── .env.example                  # 환경 변수 예시
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.js
```

## 특징

### AI 기반 추천
Google Gemini AI가 인테리어 전문가의 관점에서 공간과 스타일에 맞는 최적의 컬러 조합을 제안합니다.

### Pantone 컬러 시스템
디자인 업계 표준인 Pantone 컬러 코드를 제공하여 실제 작업에 바로 활용 가능합니다.

### 사용자 친화적 UI
직관적인 인터페이스와 반응형 디자인으로 어떤 기기에서도 편리하게 사용할 수 있습니다.

## 라이선스

MIT License

## 제작자

효정님을 위해 만든 프로젝트입니다 💝

---

**Made with ❤️ for 효정**
